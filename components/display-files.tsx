type DisplayFilesProps = {
  files: File[];
  setFiles: React.Dispatch<React.SetStateAction<File[]>>;
};

export default function DisplayFiles({ files }: { files: File[] }) {
  return (
    <div>
      <div>Hello</div>
    </div>
  );
}
